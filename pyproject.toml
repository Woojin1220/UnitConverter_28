[project]
name = "unit-converter"
version = "0.1.0"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = [
    "tests/entity",
    "tests/control",
    "tests/boundary",
]
pythonpath = ["src"]
